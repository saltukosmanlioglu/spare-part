import React from "react";

import { MainSlickProps } from "./types";
import * as Styled from "./main-slick.styled";

const MainSlick: React.FunctionComponent<MainSlickProps> = () => {
  return (
    <Styled.MainSlick>
      <div></div>
    </Styled.MainSlick>
  );
};

export default MainSlick;
